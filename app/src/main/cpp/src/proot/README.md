# PRoot runtime

The build workflow extracts the ARM64 Android PRoot executable from the official
Termux package repository and packages it as an application asset. A separately
installed Termux application is not used at runtime.
